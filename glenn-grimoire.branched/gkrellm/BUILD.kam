(
  make                                   &&
  prepare_install                        &&
  make install                           &&
  mkdir -p /usr/share/$SPELL             &&
  cp example.list /usr/share/$SPELL

) > $C_FIFO 2>&1